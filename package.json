{
  "name": "f1-video-tracker",
  "version": "1.0.0",
  "description": "Automatically track and display Formula 1 YouTube videos",
  "main": "scripts/fetch-videos.js",
  "scripts": {
    "fetch": "node scripts/fetch-videos.js",
    "fetch-archive": "node scripts/fetch-archive-2025.js",
    "dev": "npx -y serve public -p 8000",
    "build": "if [ -n \"$YOUTUBE_API_KEY\" ]; then npm run fetch; else echo \"Skipping video fetch: YOUTUBE_API_KEY not set. Using committed public/data/*.json.\"; fi",
    "validate-calendar": "node scripts/validate-calendar.js"
  },
  "dependencies": {
    "axios": "^1.13.2"
  },
  "keywords": [
    "formula1",
    "youtube",
    "static-site"
  ],
  "author": "",
  "license": "MIT"
}
